# Protocol Notes

See [../docs/controller-protocol.md](../docs/controller-protocol.md) for the canonical first-pass protocol definition.

This file exists as a local pointer inside the controller directory so controller work can stay anchored to the shared protocol doc.
